import { defineConfig } from "vite";
import { resolve } from "path";


{{framework.imports}}

// https://vitejs.dev/config/
export default defineConfig({
  plugins: [
    {{framework.plugins}},
  ],
  root: resolve("./{{frontend_path}}"),
  base: "/static/",
  server: {
    host: "localhost",
    port: 5173,
    open: false,
    watch: {
      usePolling: true,
      disableGlobbing: false,
    },
  },
  resolve: {
    resolve: {
      extensions: {{framework.extensions}},
    },
  },
  build: {
    outDir: resolve("./static/dist"),
    assetsDir: "",
    manifest: true,
    emptyOutDir: true,
    target: "es2015",
    rollupOptions: {
      input: {
        main: resolve("./{{frontend_path}}/main.{{main_ext}}"),
      },
      output: {
        chunkFileNames: undefined,
      },
    },
  },
});
